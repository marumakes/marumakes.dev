<section class="hero wrap">
	<p class="eyebrow">maru · software engineer</p>
	<h1>I'm Maru & I like making things.</h1>
	<p class="dek">
		Full-stack, with a genuine soft spot for front-end and UI craft. A handful of real
		projects below — plus a completely separate D&amp;D habit that occasionally leaks
		into articles.
	</p>
	<div class="cta-row">
		<a class="cta" href="#projects">See the work ↓</a>
		<a class="cta cta-quiet" href="/creative">or the creative side →</a>
	</div>
</section>

<style>
	.hero {
		padding-block: 3rem 5rem;
	}
	.eyebrow {
		font-family: var(--font-mono);
		font-size: 0.8rem;
		letter-spacing: 0.08em;
		text-transform: uppercase;
		color: var(--ink-faint);
		margin-bottom: 0.75rem;
	}
	h1 {
		font-size: clamp(2.25rem, 5vw, 3.25rem);
		max-width: 18ch;
	}
	.dek {
		margin-top: 1.25rem;
		max-width: 52ch;
		font-size: 1.1rem;
		color: var(--ink-soft);
	}
	.cta-row {
		display: flex;
		gap: 1.5rem;
		align-items: center;
		margin-top: 2rem;
	}
	.cta {
		text-decoration: none;
		font-weight: 600;
		color: var(--accent);
	}
	.cta-quiet {
		font-weight: 400;
		color: var(--ink-soft);
	}
	.cta:hover {
		text-decoration: underline;
	}
</style>
